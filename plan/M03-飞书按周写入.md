# M03 — 飞书写入（每视频一篇）

> 技术依据：见 `plan/技术方案.md` §4。

## 目标

固定知识库下**每个视频单独一篇** docx（元信息 + DeepSeek 正文）。

## 实现要点

1. `feishu_client.py`：子进程 `lark-cli` 创建 wiki 节点 + 写入 docx。
2. `create_video_document(title, url, time, body_md)`：每视频新建一篇，标题带日期前缀。

## 飞书控制台

- 应用 `cli_aa9fa2f1fbf8dcb1` 已创建；确认 docx、wiki 权限已发布。
- 将应用加入目标知识库可编辑。

## 验收

| # | 操作 | 通过标准 |
| --- | --- | --- |
| 1 | 创建测试视频文档 | 返回可打开 URL |
| 2 | 第二次不同视频 | 另一篇 document_id，非追加 |

## 完成标志

- [x] 固定知识库内可见测试内容
