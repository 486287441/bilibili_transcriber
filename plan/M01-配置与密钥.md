# M01 — 配置与密钥

> 技术依据：见 `plan/技术方案.md` §2、§3。

## 目标

本地安全存放 DeepSeek / 飞书 wiki 定位；启动时校验缺失项。

## 实现要点

1. `config.py`：`load_dotenv()`，导出 `DEEPSEEK_*`、`FEISHU_WIKI_*`。
2. `validate()`：缺项时中文报错退出。
3. `requirements.txt` 增加：`openai`、`python-dotenv`、`httpx`。
4. `.env` 已有 DeepSeek；补全 `FEISHU_WIKI_SPACE_ID`、`FEISHU_WIKI_PARENT_NODE_TOKEN`。

## 验收

| # | 操作 | 通过标准 |
| --- | --- | --- |
| 1 | `python -c "from config import validate; validate()"` | 缺 wiki 参数时明确报错 |
| 2 | 填全 `.env` 后再运行 | 无报错 |

## 完成标志

- [x] `config.py`、`.env.example` 就绪
