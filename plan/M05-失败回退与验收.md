# M05 — 失败回退与总验收

> 技术依据：`plan/技术方案.md`；PRD §7。

## 目标

DeepSeek/飞书失败时不丢转写，回退豆包；满足 PRD 全部成功标准。

## 实现要点

1. `fallback_to_doubao(raw_text)`：原 `full_prompt` + 剪贴板 + `start doubao`。
2. 主流程 `except` 打印错误原因。
3. 可选：`downloads/last_transcript.txt` 备份。

## 验收

| # | 操作 | 通过标准 |
| --- | --- | --- |
| 1 | 无效 DeepSeek Key | 转写成功 + 豆包回退 |
| 2 | 无效飞书配置 | 同上 |
| 3 | 全配置正确，连续 2 个视频 | 各生成独立飞书文档 + 浏览器打开 |

## 完成标志

- [x] PRD §7 全部勾选
- [x] 成功后 `webbrowser` 打开飞书文档链接
