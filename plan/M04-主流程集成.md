# M04 — 主流程集成

> 技术依据：见 `plan/技术方案.md` §4 数据流。

## 目标

转写成功后自动 DeepSeek → 飞书；打印链接；采集视频元数据。

## 实现要点

1. `download_bilibili_audio` 返回 `(audio_path, {title, url})`。
2. `postprocess_and_publish(raw_text, **meta)` 串联 M02 + M03。
3. 替换原 `full_prompt` + 豆包成功路径；保留 M05 回退。
4. 启动时 `config.validate()`。

## 验收

| # | 操作 | 通过标准 |
| --- | --- | --- |
| 1 | 短 BV 链接端到端 | 三阶段日志 + 飞书链接 |
| 2 | 第二个视频 | 另一篇独立文档 |
| 3 | 成功路径 | 未打开豆包 |

## 完成标志

- [x] 主路径无需手工粘贴
