# B 站转录 → DeepSeek → 飞书文档 — 产品需求（PRD）

> 由 `粗糙plan.md` 整理，并经 `对话 html/index.html` 产品选题更新（`choices.json` 2026-05-26）。  
> **技术栈与实现路线** → 见 `plan/技术方案.md`。

---

## 1. 背景与目标

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 要解决的问题 | [已定] | 转写完成后需手动复制到豆包，无法一键沉淀到飞书知识库 |
| 成功时用户得到什么 | [已定] | 转写经 AI 润色并附摘要后，每个视频自动写入固定知识库下的一篇独立文档；命令行输出文档链接 |

---

## 2. 用户与典型场景

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 目标用户 | [已定] | 使用本工具的本人：批量刷 B 站课程/访谈，需要笔记归档 |
| 典型场景 | [已定] | 复制 BV 链接 → 自动转写 → DeepSeek 润色+摘要 → 新建飞书文档；失败则回退剪贴板+豆包 |

---

## 3. 功能范围（用户能做什么）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 核心流程 | [已定] | 监听剪贴板 B 站链接 → 转写 → DeepSeek 处理 → 在固定知识库下新建该视频文档 |
| 关键页面/状态 | [已定] | 命令行分阶段日志：转写 / DeepSeek / 飞书；成功打印飞书 URL |
| 边界与例外 | [已定] | API/飞书失败：走豆包回退，不丢转写原文；不增加 GUI 确认步骤 |

---

## 4. 体验与交互原则

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 交互方式 | [已定] | 仍以「复制链接」触发，转写结束全自动后续链路 |
| 视觉/氛围期望 | [已定] | 命令行简洁日志 |
| 进度与反馈 | [已定] | 三阶段状态明确；失败时说明已进入回退模式 |

---

## 5. 明确不做（范围外）

| 项 | 说明 |
| --- | --- |
| 飞书内协同编辑、评论、审批流 | 仅创建/更新文档内容 |
| 多用户 SaaS | 个人工具，单租户飞书应用 |
| 每次转写前人工预览 | 已选「直接上云」，不做默认预览门控 |
| 保留豆包为主路径 | 已选完全取代；仅失败时回退 |

---

## 6. 产品决策记录（HTML 提交后填写）

| 题号 | 问题（产品向） | 选择 | 用户可理解含义 |
| --- | --- | --- | --- |
| q_trigger_moment | 转写完成后何时写飞书？ | `auto_immediate` | 转写结束立即 DeepSeek + 写飞书，无需确认 |
| q_doc_content | 飞书里放什么？ | `polish_plus_summary` | 纠错/简体/分段/文首极简目录（3～5 条一级标题）+ 文末「核心观点摘要」 |
| q_doc_structure | 多视频如何组织？ | `one_video_one_doc` | 每个视频单独一篇飞书文档，标题带日期前缀，归档在同一知识库目录下 |
| q_feishu_destination | 落在飞书哪里？ | `fixed_wiki` | 固定知识库/空间，配置一次后始终写入 |
| q_failure_experience | 失败时？ | `fallback_clipboard_doubao` | 保留转写；复制原豆包指令到剪贴板并打开豆包 |
| q_replace_doubao | 与豆包关系？ | `replace_fully` | 成功路径不再打开豆包；仅失败回退 |
| q_privacy_scope | 上传前预览？ | `always_cloud` | 不预览，直接调用 DeepSeek 与飞书 |
| q_metadata_in_doc | 文档抬头？ | `rich_header` | 视频标题 + B 站链接 + 转写日期时间 |

---

## 7. 成功标准（需求级验收）

1. 复制 B 站链接并完成转写后，**无需额外操作**，飞书中出现**一篇新文档**：含抬头元信息、润色正文、极简目录与核心观点摘要。
2. 每个视频对应**独立一篇**文档，不再合并为周记。
3. 文档落在用户配置的**固定知识库**下，命令行打印可打开的链接。
4. DeepSeek 或飞书任一步失败时：**不丢转写**，自动回退剪贴板 + 豆包；成功路径**不**打开豆包。
5. 命令行可见「转写 → AI → 飞书」三阶段进度。

---

## 8. 技术实现（见 plan）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 技术栈 | 见 plan | Python、`openai` 兼容 DeepSeek、飞书开放平台 docx/wiki API |
| 部署与运行 | 见 plan | 本地 `.env` 密钥，沿用现有启动方式 |
| 数据与存储 | 见 plan | 本地 `state/week_docs.json` 记录「周 → 飞书 document_id」 |
