# GPU 转写（RTX 等）：先装 CUDA 版 PyTorch，再装其余依赖
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
funasr>=1.3.26
modelscope
sentencepiece==0.2.0
# 画面硬字幕：PaddleOCR 3.x + PP-OCRv5。本项安装 CPU 推理引擎；
# 如需 CUDA，请按本机 CUDA 版本用 paddlepaddle-gpu 替换 paddlepaddle。
paddlepaddle>=3.0,<4
paddleocr>=3.0,<4
yt-dlp[default]
pyperclip
tqdm
openai
tokenizers>=0.15.0
python-dotenv
fastapi
uvicorn[standard]
httpx
socksio
websocket-client
