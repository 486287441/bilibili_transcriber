好的,这个拆分思路是对的——后端先把"服务化"和"算法"这两块硬骨头啃下来,前端本质上就是一个状态展示+操作控制面板,会简单很多。下面按你的五个功能点分别拆,最后再补一个"通用基础设施"部分,这部分往往是前后端都依赖、但容易被遗漏的。

**整体架构前提**

无论最终选 FastAPI+静态页面还是 Tauri,核心思路都是:后端是一个常驻进程,负责剪贴板监听、队列调度、下载、转录、AI润色、数据持久化,并对外暴露一套 HTTP API(用于操作类请求,比如手动添加链接、删除队列项)和一个 WebSocket(用于推送实时状态,比如队列变化、进度更新)。前端只做订阅和展示,不承担任何业务逻辑。这个分工定下来之后,下面五个功能基本都能按"后端做什么、前端做什么"清晰对应。

**功能1:自启动与后台运行**

后端需要做的是把现有的流水线脚本重构成一个长期运行的服务进程,绑定固定端口提供 API。这一步还包括两件事:一是写一个注册/取消开机自启的逻辑(Windows 下可能是写注册表启动项或任务计划程序,需要做成可被 API 调用的开关,而不是用户手动改);二是资源调度策略,比如 ASR 模型是常驻显存还是空闲一段时间后自动卸载、有新任务再加载,这个决定会影响服务启动后的内存/显存占用情况,值得在后端先想清楚。

前端在这块的工作很轻,基本就是一个设置页面里的开关——"开机自启"toggle,点击后调用后端 API 完成注册/取消;再加一个服务状态指示(比如显示"后台服务运行中"),方便用户确认。

**功能2:剪贴板监测与自动转文字**

这部分前面提到过,核心逻辑必须在后端。后端要做:剪贴板监听循环(你现有流水线里应该已经有这部分代码)、链接识别(目前是 B 站,要不要扩展到 yt-dlp 支持的其他站点)、去重判断(链接是否已在队列或历史记录中存在)、以及检测到新链接后自动调用 metadata 拉取(获取标题、时长)并入队,同时通过 WebSocket 推送"新任务已添加"事件。另外建议提供一个"手动添加链接"的 API 接口,作为剪贴板监听失效时的备用入口。

前端需要做的是:一个手动粘贴/输入链接的入口(调用上面那个手动添加 API);一个剪贴板监听开关(如果用户不想让它自动抓取,可以临时关闭,调用后端开关 API);以及监听 WebSocket 推送,当后端检测到新链接时实时把它加进界面的队列展示里,不需要前端自己做任何剪贴板相关的事。

**功能3:队列管理系统**

这是后端工作量比较大的一块。需要设计任务的状态机(排队中、下载中、转录中、润色中、已完成、失败),并把队列持久化到 SQLite(而不是纯内存),这样服务重启后排队和处理中的任务能恢复。还要确定并发策略——下载和 ASR 是否分开看待(下载可并行、ASR 大概率串行,取决于你显卡能不能跑多个 ASR 模型实例),以及失败任务的重试逻辑(自动重试几次、超过后标记失败并允许手动重试)。这些都需要在后端 API 层暴露出来:获取队列列表、删除/调整顺序、手动重试某个任务。

前端这块就是一个队列列表组件,展示每个任务的标题、状态、所在位置,并提供删除、重试这些操作按钮,点击后调用对应后端接口。配合 WebSocket 推送实现队列状态的实时刷新。

**功能4:界面布局与进度显示(算法部分主要在后端)**

这是整个改动里最有技术含量的一块,几乎全部工作量在后端。建议把整体进度拆成三个阶段分别建模:下载阶段、ASR转录阶段、AI分段+DeepSeek润色阶段。下载阶段可以基于已下载字节数和实时速率动态估算剩余时间;ASR阶段建议积累历史任务数据(音频时长 vs 实际处理耗时),统计出一个"处理速率系数",随样本增多动态校准,这样能比单纯按切片数量估算准得多;润色阶段可以基于转写文本的 token 数量和 DeepSeek API 的历史响应时间估算。

后端需要做的具体工作:在流水线的每个阶段插入进度上报点(不仅是当前阶段内部的进度,还要按照各阶段预估耗时占比换算成全局百分比)、维护一个历史耗时数据表用于动态校准估算模型、在任务开始前尽早通过 yt-dlp 拉取 metadata(标题、时长)以便从第一秒就能给出预估、以及通过 WebSocket 持续推送"当前阶段、全局百分比、预计剩余时间"这三个核心数据。

前端则相对轻量:中间核心区域展示当前任务的链接、标题、时长(这些数据后端早期就能拿到);进度条组件本身建议做"虚拟插值"动画——后端每次推送一个真实进度点,前端用 CSS transition 或 requestAnimationFrame 在两次真实数据之间让进度条平滑过渡,而不是阶梯跳变,这部分纯粹是前端的视觉处理,不需要后端额外配合;再加一个剩余时间的文字展示,直接显示后端算好的数值即可。

**功能5:历史转录记录**

后端需要在 SQLite 里建一张历史记录表,字段包括标题、链接、时长、处理时间、状态(成功/失败)、输出结果(飞书文档链接、转写文本)。需要的 API 包括:分页/筛选查询历史列表、获取单条详情、对某条记录重新触发处理(比如以后调整了 DeepSeek 的 prompt,想用新 prompt 重新跑一遍润色而不重新下载转录)、删除记录。另外原始音视频文件的保留策略(处理完自动删除还是保留)也建议在后端做成可配置项,避免磁盘被占满。

前端是一个历史记录列表/表格组件,支持按标题或日期搜索筛选,点击可查看详情(转写文本预览、飞书链接),并提供"重新处理"和"删除"按钮调用对应后端接口。  
**功能6：休眠功能**

目前的大模型加载方式是等到第一次任务出现时再加载。

建议把它改成这样：

1. 设置一个 30 分钟的检测机制，如果没有操作或者没有新的连接进来，就直接进入休眠状态，把大模型取消加载。

2. 等有新任务时再重新加载。

通过这种方式，可以降低平时对显存的占用。

**通用基础设施(前后端都依赖,但容易被漏掉)**

后端这边还需要补充:统一的 API/WebSocket 设计本身(比如用 FastAPI + WebSocket,这个框架选择会影响所有上面的接口形式)、配置管理(DeepSeek API key、飞书 webhook 等敏感信息的存储方式,建议本地加密文件而不是明文)、如果计划支持局域网内手机访问,还需要一个简单的 token 认证机制。

前端这边对应的是:一个设置页面,用于配置上面这些 API key 和各种开关(自启动、剪贴板监听);WebSocket 连接管理(断线重连逻辑);如果要支持手机访问,布局上需要做响应式适配。

这样拆下来,你可以先专注把后端的"服务化改造"(常驻进程+API/WebSocket暴露)、"队列持久化"和"进度估算算法"这三块做完,这是真正的硬骨头,做完之后前端基本就是把这些已经算好、推送好的数据接到界面上展示和操作,工作量和不确定性都会小很多。如果你想,我们可以挑其中一块(比如进度估算算法或者队列状态机设计)再往下细化具体的数据结构和接口定义。
